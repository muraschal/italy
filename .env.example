# Routing-Backends für scripts/fetch-routes.ts — beide optional.
# Ohne Eintrag werden die öffentlichen Demoserver verwendet (kein API-Key nötig).

# OSRM — Autoetappen
OSRM_BASE_URL=

# Valhalla — Fusswege (der OSRM-Demoserver kann kein Fussgängerprofil)
VALHALLA_BASE_URL=
